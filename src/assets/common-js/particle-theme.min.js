$(document).ready(function() {
  
    $('a.page-scroll').bind('click', function(e) {
        var $anchor=$(this);
        $('html, body').stop().animate( {
            scrollTop: $($anchor.attr('href')).offset().top
        }
        , 1500, 'easeInOutExpo');
        e.preventDefault()
    });



    $('.counter').counterUp( {
        delay: 20, time: 2200
    });
    
    new WOW().init();      

    if ($(window).width() >= 768){
      $('.dropdown').on('mouseover', function(e){
          $(this).find("ul:first").show();
      }).on('mouseout', function(e){
         $(this).find("ul:first").hide();
      });
    }
    


    $(window).scroll(function() {    
    var scroll = $(window).scrollTop();    
        if (scroll >= 150) {
            $("#aptagrim_menu.navbar").addClass("darkHeader");
           
        } else {
            $("#aptagrim_menu.navbar").removeClass("darkHeader");
        }
    });

    $(window).load(function() {    
    var scroll = $(window).scrollTop();    
        if (scroll >= 150) {
            $("#aptagrim_menu.navbar").addClass("darkHeader");
           
        } else {
            $("#aptagrim_menu.navbar").removeClass("darkHeader");
        }
    });
    

      $('.smooth_scroll').click(function(event) {
        var id = $(this).attr('href');
        $('html, body').stop().animate({
                scrollTop: $(id).offset().top-70
        },1000);
        event.preventDefault();
    });


      $('.smooth_scroll_100').click(function(event) {
            alert($(this).attr('href'));
            var id = $(this).attr('href');            
            $('html, body').stop().animate({
                    scrollTop: $(id).offset().top-500
            },1000);
            event.preventDefault();
        });

      // $(function() {
      //     var pgurl = window.location.href.substr(window.location.href
      //        .lastIndexOf("/") + 1);
      //        if ("index.html#about_us" == pgurl)
      //        {
      //          $("#about_us").css("padding-top",70);
      //         scroll();
      //       }
      //      else
      //       {
      //       $("#about_us").css("padding-top",0);
      //       }

      //       function scroll(){
      //           $(window).scroll(function() { 
      //               $("#about_us").css("padding-top",0);
      //          });
      //       }
      //  });

      $(function() {
          var pgurl = window.location.href.substr(window.location.href
             .lastIndexOf("/") + 1);

                 if ("index.html#about_us" == pgurl)
                    {                
                        $('html, body').stop().animate({
                                scrollTop: $("#about_us").offset().top-70
                        },1000);
                    }               

                    
       });
})